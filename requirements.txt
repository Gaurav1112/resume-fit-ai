# Runtime for the default local engine. No API key, no network, no model.
# Kept deliberately slim so it fits a serverless bundle.
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7

# Document intake
pypdf>=4.2
python-docx>=1.1

# Document output
reportlab>=4.2
